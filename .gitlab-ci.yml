variables:
  PACKAGE_PATH: /go/src/gitlab.com/iren.vasilevna/catalogue
 
  # https://docs.gitlab.com/ee/ci/docker/using_docker_build.html
  CONTAINER_TEST_IMAGE: $CI_REGISTRY_IMAGE:$CI_COMMIT_REF_SLUG
  CONTAINER_RELEASE_IMAGE: $CI_REGISTRY_IMAGE:latest
  GROUP: weaveworksdemos
  COMMIT: $CI_BUILD_REF
  TAG: $CI_COMMIT_TAG 
  REPO: catalogue
  MY_COOL_REGISTY: 10.128.0.9:5000
  GITLAB_OVERLAY: 1

  DOCKER_TLS_CERTDIR: "/certs"
  #DOCKER_HOST: tcp://localhost:2375
  #DOCKER_HOST: tcp://10.128.0.9:2376
  #DOCKER_DRIVER: overlay2

image: $MY_COOL_REGISTY/bios:dc-ssh
#image: golang:1.14.1-alpine
services:
  - name: docker:dind
    entrypoint: ["dockerd-entrypoint.sh"]
    command:  ["--insecure-registry=10.128.0.9:5000"]


    ############################ StageS
stages:
# make all needs microservices containers for integrated tests
#  - create_containers
#
  - build
  
  - test
  - create_containers
# make go environment, build test_container, make unit tests, integration mock tests, sends to artifactory
#  - stage
# release for human rewiev
#  - deploy
# manual deploy to prod
 
# A hack to make Golang-in-Gitlab happy
# .anchors:
#   - &inject-gopath
#       mkdir -p $(dirname ${PACKAGE_PATH})
#       && ln -s ${CI_PROJECT_DIR} ${PACKAGE_PATH}
#       && cd ${PACKAGE_PATH}


    ############################ A hack to make Golang-in-Gitlab happy
.go_dind_gitlab_hack: &go_dind_gitlab_hack
  - 'export SHARED_PATH="$(dirname ${CI_PROJECT_DIR})/shared"' 
  - echo $SHARED_PATH
  - rm -rf ${SHARED_PATH}/*
  - ls -a ${SHARED_PATH}
  - mkdir -p ${SHARED_PATH}  && ln -s ${CI_PROJECT_DIR} ${SHARED_PATH}
  - 'export PACKAGE_PATH="$(dirname ${SHARED_PATH})/catalogue"' 
    ############################

    ############################ Stage
create_containers:
  stage: create_containers
#  tags:
#    - dev
  # only:
  #   - branches
 # except:
 #   - master
 #  dependencies:
 #   - dep

  script: 
    - echo $repo_user
    - echo $DOCKER_HOST

  after_script:
    - docker ps
    - docker images

    ############################ Stage
build:
  stage: build
  image: $MY_COOL_REGISTY/bios:dc-ssh
  before_script:
    - docker login -u$repo_user -p$repo_pass $MY_COOL_REGISTY

  script:
    - set -e
    - *go_dind_gitlab_hack
    
    #- go get -u github.com/FiloSottile/gvt
    # - go env GOPATH    
    #- gvt restore

    - ./scripts/build.sh;

    - docker tag weaveworksdemos/catalogue-db 10.128.0.9:5000/bolvan:$CI_COMMIT_REF_SLUG
    - docker tag weaveworksdemos/catalogue 10.128.0.9:5000/bolvan:$CI_COMMIT_REF_SLUG

    #- docker push 10.128.0.9:5000/bolvan:$CI_COMMIT_REF_SLUG

  after_script:
    - docker ps
    - docker images

    ############################ Stage
test:
  stage: test
  image: $MY_COOL_REGISTY/bios:dc-ssh
  before_script:
    - docker login -u$repo_user -p$repo_pass $MY_COOL_REGISTY

  script:
    - set -e
    - apk add sudo
    - *go_dind_gitlab_hack
    
    #- go get -u github.com/FiloSottile/gvt
    # - go env GOPATH    
    #- gvt restore


    #- ./scripts/build.sh;
    # unit
    - ./test/test1.sh unit.py
    # integr MySql, API
    - ./test/test1.sh container.py --tag $TAG


  after_script:
    - docker ps
    - docker images

